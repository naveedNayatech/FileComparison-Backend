var excel_compare = require("../");

var options = {
    file1: '../data/school.xlsx', // file1 is the main excel to compair with
    file2: '../data/all.xlsx', // file2 is the file for compair
    column_file1: {
        column: [1, 2],
        join: '-'
    },
    column_file2: {
        column: [1],
        join: ''
    }
}

console.log(excel_compare(options))
